<Application x:Class="ShieldX.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ShieldX"
             xmlns:conv="clr-namespace:ShieldX.Converters"
             >
    <Application.Resources>
        <!-- Main Theme Resources (DynamicResource for instant switching) -->
        <SolidColorBrush x:Key="AppBg"         Color="#0D1117"/>
        <SolidColorBrush x:Key="CardBg"        Color="#161B27"/>
        <SolidColorBrush x:Key="CardBg2"       Color="#12161F"/>
        <SolidColorBrush x:Key="SidebarBg"     Color="#0A0D14"/>
        <SolidColorBrush x:Key="TitleBg"       Color="#0D1117"/>
        <SolidColorBrush x:Key="TextPrimary"   Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#718096"/>
        <SolidColorBrush x:Key="TextMuted"     Color="#4A5568"/>
        <SolidColorBrush x:Key="BorderClr"     Color="#2D3748"/>
        <SolidColorBrush x:Key="InputBg"       Color="#0D1117"/>
        <SolidColorBrush x:Key="HoverBg"       Color="#1E2433"/>
        <SolidColorBrush x:Key="NavHover"      Color="#161B27"/>
        <SolidColorBrush x:Key="NavActive"     Color="#0D2818"/>
        <SolidColorBrush x:Key="RowBg"         Color="#0D1117"/>
        <SolidColorBrush x:Key="AltRowBg"      Color="#111827"/>
        <SolidColorBrush x:Key="GridLine"      Color="#1E293B"/>
        <SolidColorBrush x:Key="AccentClr"     Color="#00E5CC"/>
        <SolidColorBrush x:Key="DataGridHdr"   Color="#0D1117"/>

        <!-- Legacy Theme Resources (updated dynamically) -->
        <SolidColorBrush x:Key="AppBackground" Color="#0D1117"/>
        <SolidColorBrush x:Key="CardBackground" Color="#161B27"/>
        <SolidColorBrush x:Key="CardBackground2" Color="#12161F"/>
        <SolidColorBrush x:Key="SidebarBackground" Color="#0A0D14"/>
        <SolidColorBrush x:Key="BorderColor" Color="#2D3748"/>
        <SolidColorBrush x:Key="InputBackground" Color="#0D1117"/>
        <SolidColorBrush x:Key="HoverBackground" Color="#1E2433"/>
        <SolidColorBrush x:Key="AccentColor" Color="#00E5CC"/>
        <SolidColorBrush x:Key="TitleBarBackground" Color="#0D1117"/>
        <SolidColorBrush x:Key="NavButtonHover" Color="#161B27"/>
        <SolidColorBrush x:Key="NavButtonActive" Color="#0D2818"/>
        <SolidColorBrush x:Key="RowBackground" Color="#0D1117"/>
        <SolidColorBrush x:Key="AltRowBackground" Color="#111827"/>
        <SolidColorBrush x:Key="GridLineColor" Color="#1E293B"/>

        <!-- Modern Color Palette -->
        <Color x:Key="BackgroundDeep">#FF080C14</Color>
        <Color x:Key="BackgroundCard">#FF0D1421</Color>
        <Color x:Key="BackgroundSurface">#FF111B2E</Color>
        <Color x:Key="AccentPrimary">#FF00D4AA</Color>
        <Color x:Key="AccentSecondary">#FF6C63FF</Color>
        <Color x:Key="AccentDanger">#FFFF4757</Color>
        <Color x:Key="AccentWarning">#FFFFA502</Color>
        <Color x:Key="AccentSuccess">#FF2ED573</Color>
        <Color x:Key="TextPrimaryColor">#FFE8F4F8</Color>
        <Color x:Key="TextSecondaryColor">#FF8899AA</Color>
        <Color x:Key="TextTertiary">#FF667788</Color>
        <Color x:Key="TextMutedColor">#FF4A5568</Color>
        <Color x:Key="GlowCyan">#3300D4AA</Color>
        <SolidColorBrush x:Key="GlowCyanBrush" Color="{StaticResource GlowCyan}" />

        <!-- Brushes -->
        <SolidColorBrush x:Key="BackgroundDeepBrush" Color="#080C14" />
        <SolidColorBrush x:Key="BackgroundCardBrush" Color="#0D1421" />
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#0D1421" />
        <SolidColorBrush x:Key="BackgroundSurfaceBrush" Color="#111B2E" />
        <SolidColorBrush x:Key="AccentPrimaryBrush" Color="#00E5CC" />
        <SolidColorBrush x:Key="AccentSecondaryBrush" Color="{StaticResource AccentSecondary}" />
        <SolidColorBrush x:Key="AccentDangerBrush" Color="{StaticResource AccentDanger}" />
        <SolidColorBrush x:Key="AccentWarningBrush" Color="{StaticResource AccentWarning}" />
        <SolidColorBrush x:Key="AccentSuccessBrush" Color="{StaticResource AccentSuccess}" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#E8F4F8" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#718096" />
        <SolidColorBrush x:Key="TextTertiaryBrush" Color="{StaticResource TextTertiary}" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="{DynamicResource TextMuted}" />
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="#080C14" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource AccentPrimary}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{DynamicResource TextSecondary}" />
        <SolidColorBrush x:Key="BorderPrimaryBrush" Color="{DynamicResource TextSecondary}" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBg}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="Margin" Value="0,0,0,20" />
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentClr}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentClr}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=Transparent}" CornerRadius="8" BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=Transparent}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="8,2" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AppBg}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource TextSecondary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=Transparent}" CornerRadius="8" BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=Transparent}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="8,2" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}" />

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}" />

        <!-- Theme Toggle Button Style -->
        <Style x:Key="ThemeButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Glassmorphism Card Style -->
        <Style x:Key="GlassCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBg}"/>
            <Setter Property="BorderBrush" Value="#FF1E3A5F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#3300D4AA" BlurRadius="15" ShadowDepth="0" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Enhanced TextBlock Styles -->
        <Style x:Key="HeadingLarge" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="HeadingMedium" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="HeadingSmall" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="BodyText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="LineHeight" Value="1.5"/>
        </Style>

        <Style x:Key="CaptionText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Opacity" Value="0.85"/>
        </Style>

        <!-- Enhanced Button with Better Hover/Press States -->
        <Style x:Key="PrimaryButtonPolished" TargetType="Button">
            <Setter Property="Background" Value="#00E5CC"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="8" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonPolished" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2D3748"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="8" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1A2D3F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0F1B2D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Toggle Button Style -->
        <Style x:Key="ModernToggle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Border" Background="{DynamicResource AppBg}" CornerRadius="15" BorderThickness="2" BorderBrush="{DynamicResource TextSecondary}">
                            <Grid>
                                <Ellipse x:Name="Knob" Fill="{DynamicResource TextSecondary}" Width="20" Height="20" HorizontalAlignment="Left" Margin="3"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="30,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentClr}"/>
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource GlowCyanBrush}"/>
                                <Setter TargetName="Knob" Property="Fill" Value="{DynamicResource AccentClr}"/>
                                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Animated Progress Bar -->
        <Style x:Key="AnimatedProgressBar" TargetType="ProgressBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{DynamicResource AppBg}" CornerRadius="4" BorderThickness="1" BorderBrush="{DynamicResource TextSecondary}">
                            <Grid>
                                <Rectangle x:Name="PART_Track" Fill="Transparent"/>
                                <Rectangle x:Name="PART_Indicator" Fill="{DynamicResource AccentClr}" HorizontalAlignment="Left">
                                    <Rectangle.RenderTransform>
                                        <ScaleTransform ScaleX="0" ScaleY="1"/>
                                    </Rectangle.RenderTransform>
                                </Rectangle>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Value" Value="0">
                                <Setter TargetName="PART_Indicator" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0" ScaleY="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <conv:LogLevelBrushConverter x:Key="LogLevelBrushConverter" />
        <conv:ScoreToBrushConverter x:Key="ScoreToBrushConverter" />
        <conv:DateTimeConverter x:Key="DateTimeConverter" />
        <conv:InverseBoolConverter x:Key="InverseBoolConverter" />
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <conv:ImpactToBrushConverter x:Key="ImpactToBrushConverter" />
        <conv:StatusBrushConverter x:Key="StatusBrushConverter" />
        <conv:BoolToColorConverter x:Key="BoolToColorConverter" />
        <conv:StrengthColorConverter x:Key="StrengthColorConverter" />
        <conv:BoolToEyeIconConverter x:Key="BoolToEyeIconConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <conv:ZeroToVisibilityConverter x:Key="ZeroToVis"/>
        <conv:NonZeroToVisibilityConverter x:Key="NonZeroToVis"/>

        <!-- Skeleton Loader Style -->
        <Style x:Key="SkeletonLoader" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#FF1E3A5F" Offset="0"/>
                        <GradientStop Color="#FF2A4A6B" Offset="0.5"/>
                        <GradientStop Color="#FF1E3A5F" Offset="1"/>
                        <GradientStop Color="#FF1E3A5F" Offset="1.5"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="0" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsVisible" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="(Border.Background).(LinearGradientBrush.GradientStops)[0].Offset" From="0" To="1" Duration="0:0:1.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Border.Background).(LinearGradientBrush.GradientStops)[1].Offset" From="0.5" To="1.5" Duration="0:0:1.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Border.Background).(LinearGradientBrush.GradientStops)[2].Offset" From="1" To="2" Duration="0:0:1.5"/>
                                <DoubleAnimation Storyboard.TargetProperty="(Border.Background).(LinearGradientBrush.GradientStops)[3].Offset" From="1.5" To="2.5" Duration="0:0:1.5"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Page Transition Animation -->
        <Storyboard x:Key="PageTransitionIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="50,0,-50,0" To="0" Duration="0:0:0.3"/>
        </Storyboard>

        <Storyboard x:Key="PageTransitionOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.2"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0" To="-50,0,50,0" Duration="0:0:0.2"/>
        </Storyboard>

        <!-- Bold Gradient Scrollbar Thumb - No Border -->
        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border CornerRadius="10" 
                                BorderBrush="Transparent"
                                BorderThickness="0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#FFA500" Offset="0.0"/>
                                    <GradientStop Color="#FF8C00" Offset="0.3"/>
                                    <GradientStop Color="#FF6347" Offset="0.7"/>
                                    <GradientStop Color="#FF4500" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Bold Gradient ScrollBar - Vertical -->
        <Style x:Key="CustomScrollBar" TargetType="ScrollBar">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Width" Value="18"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Track -->
                            <Track Name="PART_Track" Grid.Row="0" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Background="Transparent"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}" Margin="2,3,2,3" MinHeight="50"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Background="Transparent"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Bold Gradient ScrollBar - Horizontal -->
        <Style x:Key="HorizontalCustomScrollBar" TargetType="ScrollBar">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Track -->
                            <Track Name="PART_Track" Grid.Column="0" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageLeftCommand" Background="Transparent"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}" Margin="3,2,3,2" MinWidth="50"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageRightCommand" Background="Transparent"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Apply custom scrollbar to all ScrollViewers -->
        <Style TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" Style="{StaticResource CustomScrollBar}" Value="{TemplateBinding VerticalOffset}" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Column="0" Grid.Row="1" Orientation="Horizontal" Style="{StaticResource HorizontalCustomScrollBar}" Value="{TemplateBinding HorizontalOffset}" Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Value Converters -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:ZeroToVisibleConverter x:Key="ZeroToVisibleConverter"/>
        <conv:NonZeroToVisibleConverter x:Key="NonZeroToVisibleConverter"/>
    </Application.Resources>
</Application>
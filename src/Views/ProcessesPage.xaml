<Page x:Class="ShieldX.Views.ProcessesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ShieldX.Views"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="950"
      Title="Processes"
      Background="{DynamicResource AppBg}"
      Unloaded="Page_Unloaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="20">
            <DockPanel Margin="0,0,0,20">
                <TextBlock Text="Process Monitor" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                <Button Content="Refresh" Click="RefreshButton_Click" HorizontalAlignment="Right" Margin="10,0,0,0" Padding="10,4" Background="{DynamicResource AccentClr}" Foreground="{DynamicResource TextPrimary}" BorderThickness="0"/>
            </DockPanel>

            <DataGrid x:Name="ProcessGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None" RowBackground="{DynamicResource CardBg}" AlternatingRowBackground="{DynamicResource AppBg}" Foreground="{DynamicResource TextPrimary}">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{DynamicResource CardBg}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSuspicious}" Value="True">
                                <Setter Property="Background" Value="#33FF4757"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Process" Binding="{Binding Name}" Width="2*" />
                    <DataGridTextColumn Header="PID" Binding="{Binding Id}" Width="*" />
                    <DataGridTextColumn Header="Memory" Binding="{Binding MemoryMb}" Width="*" />
                    <DataGridTextColumn Header="Threads" Binding="{Binding Threads}" Width="*" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</Page>
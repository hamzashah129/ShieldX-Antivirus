<Page x:Class="ShieldX.Views.QuarantinePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ShieldX.Views"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="950"
      Title="Quarantine"
      Background="{DynamicResource AppBg}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="20">
            <TextBlock Text="Quarantine Vault" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,20"/>

            <Border Style="{StaticResource GlassCard}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Quarantined Items" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,15"/>

                    <ListView x:Name="QuarantineListView" Height="300" Background="Transparent" BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Threat Name" Width="200" DisplayMemberBinding="{Binding ThreatName}"/>
                                <GridViewColumn Header="Original Path" Width="300" DisplayMemberBinding="{Binding OriginalPath}"/>
                                <GridViewColumn Header="Date Isolated" Width="150" DisplayMemberBinding="{Binding DateIsolated, StringFormat=yyyy-MM-dd HH:mm}"/>
                                <GridViewColumn Header="Actions" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Restore" Margin="0,0,5,0" Padding="5" Background="{StaticResource AccentSuccessBrush}" Foreground="{DynamicResource TextPrimary}" BorderThickness="0"/>
                                                <Button Content="Delete" Padding="5" Background="{StaticResource AccentDangerBrush}" Foreground="{DynamicResource TextPrimary}" BorderThickness="0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>